/// A set of enums use all over the

// ignore_for_file: constant_identifier_names

/// RequestType
enum Request {
  get,
  post,
  put,
  patch,
  delete,
}

/// Type of the Message
enum TypeOfMessageMessage {
  error,
  information,
  success,
}

enum ButtonType {
  continued,
  cancelled,
}
